Transpiles SCION Microfrontend Platform dependencies into JavaScript bundle
===========================================================================

1. run `npm run install`
2. run `npm run bundle`

The bundled JS file is written to `ch.sbb.scion.rcp.microfrontend/js/refs.js'

